Updated environment variable documentation to include comprehensive guide for all three credential delivery methods. Documents INITIAL_ADMIN_PASSWORD (recommended for automated deployments), ADMIN_PASSWORD (legacy support), and auto-generation behavior (default). Includes priority order explanation, use cases for each method, security notes, and best practices for credential management.